//! Code generation helpers for Linear GraphQL schema.

use anyhow::Result;

/// Placeholder function invoked by the `xtask` utility.
pub fn run() -> Result<()> {
    println!("linear-codegen::run placeholder");
    Ok(())
}
